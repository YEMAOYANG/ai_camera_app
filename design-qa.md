# 学生端星际界面验收

日期：2026-09-11。验收范围：学生端完整视觉重构，以及用户最新要求的「字体、按钮和整体布局缩小，尽量一屏显示」。

## 结论与遗留项

紧凑布局的最终检查没有可确认的 P0/P1/P2 视觉或交互回归。首页在 1280 × 720 CSS px 下，文档宽高实测为 1280 × 720；导航、标题、继续学习按钮、今日课程和进度提示都完整显示。

- [P3] 课程库和详情页的底部有少量可滚动空白。课程库文档高 728 px，完整课程卡底部 708.17 px；详情页文档高 740 px，折叠状态下最后一项底部 695.97 px。均以 720 px 高视口检查，主要内容和操作没有被隐藏。后续可以继续收紧末尾留白。
- 运行环境限制：真实课堂播放时，控制台记录一次 AI discussion 请求 503（2026-09-11T06:55:56Z）。课堂静态内容和播放器已实际打开并播放；此次视觉验收不能视为 AI 讨论功能验收通过。下文单列证据，不把服务失败归因于界面样式。
- 设计忠实度边界：原图为三门示例课程，实际测试账号只有一门已发布数学课；使用真实课程名称、目标和状态。最新的缩小指令有意改变原图的字号、按钮和卡片尺寸。背景和学科插图是基于原图生成的独立素材，保留构图和风格，但不是原图每个像素的复制。

## 视觉依据与比较方法

- 视觉源：`/Users/sqcopenclaw/.codex/generated_images/01a08f08-8cbf-7573-8d58-2d3cb8afcff2/exec-454d0d62-776e-4941-90e2-2d75c34b4e62.png`。
- 实现地址：`http://127.0.0.1:3000/today`，受管 Next.js 生产构建。
- 原图：1487 × 1058 px，无设备外框。
- 同尺寸实现截图：`output/student-space-qa/home-final.png`，1487 × 1058 px，CSS 视口 1487 × 1058，devicePixelRatio 1。
- 不进行比例拉伸或设备框裁切；将两张等尺寸图并排合成进行检查。全图合成在查看工具中缩小显示，因此另做 1:1 局部比较检查文字、按钮和课程卡。
- 全图比较：`output/student-space-qa/compare-home-final.png`（左为原图，右为实现）。
- 标题及主按钮局部比较：`output/student-space-qa/compare-hero-detail.png`。
- 课程卡局部比较：`output/student-space-qa/compare-course-detail.png`。
- 同主题、已登录、数学课学习中状态；实际课程名称、完成计数、课程数量与示例不同，不能据此计算有意义的逐像素匹配率。

## 比较历史与修复

1. 初始渲染：`output/student-space-qa/home-before.png`；并排证据为 `output/student-space-qa/compare-home-before.png`。
   - [P2] 原方案的字体、按钮和留白在常见较矮电脑屏幕上过大，影响首屏信息密度；用户明确要求缩小。
   - 修复：正文和导航分级收紧，主按钮从 74 px 收为 52 px，标题根据长度自适应；减少页头、卡片封面和各区块间距，增加矮屏布局规则。
   - 修复：详情页优先显示简短学习目标，完整课程介绍放入可展开的「课程介绍」，保留原文和操作。
   - 修复：配对页调整二维码与说明的横向排版及底部留白；手机底部导航与课程卡错开。
2. 修复后：同原图尺寸的 `home-final.png` 与上述全图、局部合成已经实际打开检查；另以 `home-1280.png` 验证用户要求的一屏尺寸。
   - 首页文档实测 1280 × 720，无横向或纵向溢出；主按钮实测 230 × 52 px、字号 19 px，长标题字号 39.04 px。
   - 配对页最终文档实测 1280 × 720，二维码、核对码、备用配对入口及说明全部可见。
   - 手机视口 390 × 844，课程卡底部约 722 px，底部导航顶部约 760 px，没有遮挡课程按钮；允许纵向滚动，不把长内容压缩成不可读的小字。

## 五项视觉检查

| 表面 | 最终观察与判定 |
| --- | --- |
| 字体与排版 | 保留白色粗体标题、青色学科提示和清晰的中英文无衬线层级。原图没有提供字体文件，不能证明字体家族完全一致。桌面与手机均检查实际换行；长标题缩小，正文保持可读，按钮不截字。字号变化是最新用户要求。 |
| 布局与间距 | 保留左导航、左侧课程内容、右侧大行星与下方课程条的主要构图；缩小页头、主按钮和卡片，矮屏主要操作一屏可见。长列表、展开介绍和手机页面允许自然滚动。 |
| 色彩与样式 | 午夜蓝底、白色正文、青色提示、黄绿色主操作和选中边框一致；语文暖金、英语薄荷绿的学科图像延续同一主题。文字区有暗色遮罩，二维码保留白色静区。 |
| 图像质量 | 使用独立生成的行星背景、三门学科插图与学生头像，Next Image 按尺寸输出；没有用代码图形替代主要插画。已放大比较行星裁切、卡片细节和边缘，没有明显拉伸、占位图或糊化。独立生成素材的细节与原图存在预期差异。 |
| 文案与内容 | 使用当前学生、年级、已发布课程和学习状态；课程不足三门时不填入虚构课程。保留配对、PIN、收藏、返回、重试、空状态与完整介绍的入口。界面没有加入实现过程或技术说明文案。 |

## 浏览器与交互证据

截图目录：`/Users/sqcopenclaw/Desktop/ai_camera_app/output/student-space-qa/`。

| 页面或状态 | 截图 | 检查范围 |
| --- | --- | --- |
| 今日学习 | `home-1280.png`, `home-final.png`, `home-mobile.png` | 一屏布局、真实课程数据、继续学习入口、移动端底部导航 |
| 课程库 | `library-1280.png` | 筛选入口、继续学习、课程卡及详情导航 |
| 课程详情 | `detail-1280.png` | 学习目标、课程提纲、开课与收藏入口；实际展开并收起课程介绍 |
| 自主练习 | `practice-1280.png` | 学科选择与实际无可复习内容反馈 |
| PIN 解锁 | `unlock-1280.png` | 完整面板与按键布局 |
| 配对 | `pair-1280.png`, `pair-mobile.png` | 二维码、核对码、备用配对及手机排版 |
| 课堂 | `classroom-start.png`, `classroom-desktop.png`, `classroom-1280.png` | 打开真实已发布课程，开始播放及暂停；学生外壳、教师区和播放器统一样式，作者幻灯片原内容保留 |
| 全局 404 | `not-found-1280.png` | 同主题提示与回到今天入口 |

加载和全局错误、旧版课堂及报告均已做同主题源代码适配。未强制制造所有服务器错误，也未完成整节课程或正式验收新家长授权链路。

控制台检查发现历史课堂错误 `Pi chat request failed: 503`。对应后端 `/internal/learning/openmaic/runtime/paid-call` 在 14:55:56 本地时间返回 503；同一路由在新课堂样式运行前的 11:36:24 已记录 503。没有因此执行付费模型重试或改动后端。此项保留为运行环境问题，不算入视觉通过范围。

## 工程验证

- Student Web：TypeScript 检查、ESLint、34 个测试文件 / 136 项测试通过，最终生产构建完成。
- OpenMAIC：TypeScript 及 20 项相关现有测试通过；新增 `0092-mira-orbital-student-classroom.patch`，全部 92 个补丁重放与当前运行源码一致。
- 最终受管服务检查：Backend 8000、Student Web 3000、OpenMAIC 3100、Gateway 3101 健康；课程生成 worker 保持停止，未触发新课程生成。
- `git diff --check` 通过。重构前代码已提交并推送；本次重构与紧凑布局仍在本地工作区。

## 实施检查清单

- [x] 首页、课程库、详情、练习、配对、解锁、课堂及全局状态统一星际视觉。
- [x] 字体、按钮、卡片与间距整体缩小。
- [x] 桌面首页及配对页验证为 1280 × 720 一屏；手机主要操作没有被底部导航遮挡。
- [x] 实际打开源图与渲染图的并排和局部比较，明确记录预期偏差。
- [x] React Bits 适配动画保留减少动态效果支持、卸载清理及第三方声明。
- [x] 保留真实数据、学习导航与原有 API 边界。

## 15:12 用户反馈后的复验：真实 Chrome 宽屏与动效

- 用户补充的视觉证据：`/var/folders/lq/3xj2bqbj4vd0xkysnv2vlz780000gn/T/TemporaryItems/NSIRD_screencaptureui_WhBI2a/截屏2026-09-11 15.12.42.png`。用户指出仍有滚动条、动效不明显。原验收只在部分尺寸通过，不能覆盖此次真实宽屏。
- [P2，已修复] 在用户原 Chrome 标签页测得 CSS 视口 1920 × 806，文档高度 820，宽度因滚动条变为 1905。原因是 Hero 使用固定的 `100svh - 380px`，但宽屏课程卡随屏宽增至 205 px，另有 ≥1600 px 的 Hero 最小高度。修复为桌面 Grid 按课程区实际高度分配剩余空间，移除固定 Hero 高度，不使用 `overflow:hidden` 隐藏业务内容。
- [P2，已修复] 原有特效主要在悬停和点击时出现，不满足用户对明显动态的要求。新增默认运行的远近星粒、轨道光点、周期流星、背景漂浮与鼠标视差；主按钮扫光和选中卡片边缘流光常驻。课程卡倾斜增至有界 ±7°。背景提供暂停/继续按钮，系统减少动态优先。
- 实际修复前截图：`output/student-space-qa/chrome-overflow-before.png`；修复后截图：`output/student-space-qa/chrome-motion-final.png`。二者来自同一 Chrome 用户标签页、同一真实学生及课程、同一 1920 × 806 CSS 视口。后者已实际打开检查，原设计中的字号、配色、课程内容和星际插画保持，变化是修复高度分配及叠加动态。
- 并排比较已实际打开：`output/student-space-qa/chrome-motion-comparison.png`（左旧右新）。截图工具返回的旧图为 1905 × 800 px，新图为 1920 × 806 px；旧图含滚动状态且可见区域不同，比较时仅在右侧和底部补齐画布，没有拉伸图像。此次按整体布局和实际 DOM 尺寸判定溢出，不据这两个略有裁切差异的截图做逐像素对齐结论；字体未改动，沿用前述局部字体检查。
- 最终窗口测量：1920 × 806 的文档宽高为 1920 × 806，scrollY 为 0。额外实测 1280 × 720、1599 × 720、1600 × 720、1900 × 868、1920 × 1080、701 × 720 均无横向或纵向溢出。
- 手机 390 × 844 保留自然纵向滚动，课程卡底部 722、底部导航顶部 760，暂停按钮在卡片右侧没有盖住课程操作。截图：`output/student-space-qa/chrome-motion-mobile.png`。极长移动端报告也移除了 Hero 的最大高度限制。
- 动态证据：`motion-frame-a.png` / `motion-frame-b.png` 中右侧 760 × 600 背景区域的像素确实发生变化，背景 transform 值也随时间/鼠标改变。点击暂停后 `data-space-motion=paused`，按钮扫光为 paused；`motion-paused-a.png` / `motion-paused-b.png` 同区域像素差为 0。已点击继续，最终页面保留运行状态。
- 已实际点击选中课程，确认课程入口未改变且没有新增滚动；本轮没有逐一验证鼠标停在卡片四个角的投影，也没有对所有服务异常态做浏览器注入。
- 新增测试覆盖暂停偏好、实时系统减少动态变化和卸载订阅清理。最终 TypeScript、ESLint、35 文件 / 137 项测试及受管生产构建通过。刷新后的用户 Chrome 控制台没有错误。Canvas 限 30 fps，移动端减少粒子；未宣称硬件实测帧率。

## 后续增强：课程、练习、配对与课堂的星际体验

用户认可首页后，要求其他页面也加入特效和星际氛围。此次保持已确认的紧凑布局与真实学习流程。

- 课程库与详情：共享星空背景增强，课程封面和详情场景缓慢漂浮；选中筛选流光、续学横幅光线、收藏成功后短星光；学习目标改为连接的星轨节点，不暗示这些目标已经完成。
- 自主练习：学科星球、选中能量边缘、启动和提交反馈、真实进度节点；答对与完成时在结果图标附近短暂绽放星点，答错使用温和解释。没有虚构积分、答案、进度或奖励。
- 配对/PIN：加入共享星空与接入信号，二维码仅外围亮度变化，SVG 和白色静区实测无 transform；输入完成时显示准备好的边框。PIN 仅输入测试占位并清空，没有提交或改变登录凭据。
- 课堂：只增强 Student Web 的顶部 60 px 导航，Canvas 高度实测 60，iframe 顶部实测 60。打开真实课堂时保持暂停状态，没有启动额外 AI 讨论或完整课堂生成。无需新增 OpenMAIC 补丁。
- 暂停偏好只保存一个 sessionStorage 布尔值，课程页切到练习或登录/课堂外壳后保持；系统减少动态优先，后台标签停帧。未存储身份、令牌或学习内容。

浏览器证据来自用户原 Chrome：课程库、详情、练习在 1920 × 806 下文档宽高均为 1920 × 806。截图为 `library-effects-final.png`、`detail-effects-final.png`、`practice-effects-final.png`。课程库增强前为 `library-effects-before.png`，同尺寸并排比较为 `library-effects-comparison.png`，位于上述 QA 截图目录。

配对、PIN、课堂外层另外以 1280 × 720 CSS 视口检查，文档没有溢出。该 Chrome 工具的截图为缩放后的物理浏览器画面（`pair-effects-final.png`、`unlock-effects-final.png`、`classroom-effects-final.png`），含模拟视口外的背景，不能当成 1:1 CSS 像素截图；布局结论取自实际 DOM 尺寸。

真实交互检查：收藏成功后产生星光，再取消收藏恢复原状态；暂停后跨页保持，继续后恢复；英语学科选中及开始复习入口正常。当前账号返回「当前难度还没有新的复习题」，因此本轮没有用真实题目走完答题庆祝，练习分支由现有定向测试及源代码检查覆盖，不宣称完整答题验收。

手机复验发现并修复一项 [P2]：浮动暂停按钮原本覆盖英语卡右缘。现已移入二级页面底部导航预留区。390 × 844 实测三个导航链接最右侧为 x=304，暂停按钮为 x=307..351/y=772..820，开始复习按钮底部 749.28，导航顶部 760，没有重叠；手机长内容仍自然滚动。证据 `practice-effects-mobile-final.png`。首页原有布局没有改变。

最终验证：TypeScript、ESLint、35 文件 / 138 项测试通过，修复手机位置后受管生产构建再次通过；最终用户 Chrome 控制台无错误，已恢复原窗口尺寸并保留运行中的课程库。新动效仅改 Student Web，不修改后端 API 或课程数据。

## 导航与控件精修

针对「切换、按钮、tabbar、侧栏粗糙」的反馈，保留已确认的星际场景与紧凑布局，统一交互控件。

- 左侧导航与手机底栏由根布局持久维护，三条链接和单个选中底座不会随内容重新创建；选中底座通过 320 ms CSS 位移连接位置。页面正文独立进行 220 ms 透明度进入，不使用阻挡点击的整页动画遮罩。
- 按钮统一 600 字重、细边框、14 px 圆角及悬停、按下、键盘焦点、禁用反馈。首页继续学习仍为 52 px 胶囊主按钮；低年级及兼容课堂的按钮保留至少 48 px 触控尺寸。
- 课程状态筛选改为滑动底座分段控件，学科标签使用小色点与轻底色。续学区保持独立于列表筛选，避免点击标签后整个筛选栏跳位；后端本身对 `continueItem` 不应用 bucket/subject 筛选，未改变 API。
- 身份加载及错误沿用同一视觉外壳，仍执行每次 `/api/auth/me` 检查，不缓存身份或绕过 401 跳转。动态开关正式置于导航内部，独立登录与课堂页面保留自己的开关。

### 此轮浏览器验收

- 在用户原 Chrome 标签页实测快速连续点击「练习 → 今天 → 课程」，最终进入课程页；导航与动态开关各只有一个。另有回归测试确认不同路径渲染后导航、链接和选中底座均为同一 DOM 节点。
- 筛选切换前后顶部均为 y=335.1875，控件不再上下跳动；实际点击状态与学科筛选、打开课程详情、进入真实课堂并返回。课堂保持暂停，未发起新的 AI 播放/讨论；课堂内学生侧栏数量为 0，iframe 顶部仍为 60 px。
- 原窗口为 1920 × 806，首页、课程库与详情页文档均为 1920 × 806，没有新增纵向或横向溢出。最终课程库保持背景动态运行。
- 手机 390 × 844：底栏高度 68，链接点击高度 54，动态按钮 48 × 48；三条链接最右 x=287，开关 x=304..352，无重叠。五个筛选项总宽与容器一致，最后一项右缘 x=351，小于容器右缘 x=355。320 px 窄屏改为两行筛选，所有文字可见；长内容自然滚动。
- 暂停后跨页到练习仍为 `data-space-motion=paused`，继续后恢复。系统减少动态及旧课堂最小触控覆盖通过源代码与测试检查，未更改用户系统设置。
- 原生窗口截图：`output/student-space-qa/navigation-before.png`、`navigation-final.png`、`detail-navigation-final.png`。同尺寸并排比较 `navigation-comparison.png` 已打开检查，左旧右新；图中星空位置随动画变化，不用于逐像素相似度判断。手机覆盖视口的截图工具会输出缩放后含视口外区域的画面，`navigation-mobile-final.png` 不作为 1:1 视觉依据；手机尺寸结论来自实际 DOM。

最终验证：TypeScript、ESLint、37 个测试文件 / 146 项测试通过；补充的课程筛选回归 8 项再次通过。最终受管生产构建完成，Backend、Student Web、OpenMAIC、Gateway 均健康，课程生成 worker 保持停止。Chrome 控制台无新增错误，`git diff --check` 通过。改动仍在本地工作区。

final result: passed


## 2026-09-11 课堂控件可见性与输入区修复

修复了气泡暂停按钮默认空白、低对比图标/状态点，以及底部输入框内外双重焦点边框、发送按钮紫色阴影、输入时讲解文字模糊。覆盖普通/演示两套课堂、兼容课堂和练习图标。

Student Web lint/typecheck、146测试、生产构建通过；OpenMAIC最终生产构建通过，92补丁回放与16文件字节一致。原浏览器实际展开/收起/多行输入检查通过，测试文字已清除且未发送。详细范围、检查边界和截图见 output/student-space-qa/icon-audit/audit-notes.md。


## 2026-09-11 开始上课弹窗收紧

只调整学生主题的 start-gate CSS：按钮18→14px、字重800→600、高度56→44px（粗指针触屏48px），弹窗最大宽380px、圆角16px、标题22px、说明13px，减弱发光并收紧留白。源标记静态预览实测380×230px；320/390px视口无横向溢出。生产构建通过，实际播放器CSSOM已验证14px/44px等规则已加载；既有课堂已开始，因此不会强制重置播放状态来弹出启动对话框。补丁SHA256 724dbba714d6eedefb28a56467b82f012d5054b21e816985516335e38ab4f4e5，反向补丁检查及diff检查通过。


## 2026-09-11 数学课程独立封面

通过内置 image_gen 生成金色几何探索平台，保存为 student-web/public/images/space/subject-math-lab.png；完整提示词见 output/imagegen/math-course-art-prompt.md。统一 SpaceCourseArt 替换后覆盖首页卡片、继续学习横条、课程列表、课程详情与练习科目入口，背景 hero-space.png 保持原资源。数学卡片垂直裁切调整至50%。

Student Web lint/typecheck、37测试文件/146测试及生产构建通过。已刷新用户原Chrome：课程列表与首页实测新资源加载成功，截图 math-lab-library.png 和 math-lab-today.png 位于 output/student-space-qa。
