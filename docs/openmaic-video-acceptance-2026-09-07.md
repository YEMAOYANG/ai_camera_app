# OpenMAIC 视频接入与本次样课验收

2026-09-07。本次完成了百炼视频配置和真实媒体调用，交付了**人工修正后的完整预览**。原正式自动生成任务仍为 failed，不能将本次结果表述为自动精品课流程、正式发布或与官网 demo 全面一致已通过。

预览入口：<http://127.0.0.1:3100/classroom/preview-number-sense-video-20260907>。该地址使用本机受管的 OpenMAIC 服务；3100 没有开放为局域网监听。预览没有进入阿达的学生课程目录，也没有写入学习完成记录。

## 已生效的配置

复用已有百炼服务端 Key，视频 Provider 为 `happyhorse`，模型为 `happyhorse-1.0-t2v`。图片仍用 `qwen-image-max`，讲解使用 `qwen3-tts-flash` / Ethan，语音识别核验使用 `qwen3-asr-flash`。这不是用 Qwen 对话模型直接生成视频。

正式课创建策略新增 `mira-formal-happyhorse-video.v1`：按教学需要使用，最多调用一次，5 秒、720p、16:9。没有教学需要时允许零段；本次合成样课明确要求一段情境视频。精确数量和答案由可核对的课件图元与互动承担，不依赖生成视频中的物品数量。

服务端会记录独占视频调用凭据、保存视频文件，并校验同课堂引用、字节 SHA、尺寸和真实画面时长。后端 v106 视频课程指纹、正式素材验证和学生发布门禁已接入；历史兼容契约保留。

生产已部署 patch53，BUILD_ID：`BawPT8SMEh26iF66ApFV4`。健康接口的 Pro、联网搜索、图片、视频和语音能力均为 true。旧数学/语文课程仍 ready，发布与素材门禁及 58 份旧音频校验通过。

## 原自动任务的真实结果

唯一正式生成 POST 对应 job `omformal_9b9e2b29908223a643dcd72a`，保留原请求和独立数据库记录。该任务生成了 8 页、1 段视频和 1 张图片，但在人工设置的 Pro 80 次验收调用上限处终止：

`FORMAL_PROFESSIONAL_SESSION_FAILED:MIRA_PAID_GUARD_CALL_CAP`

原任务没有进入最终质量审核或语音阶段，`review_course_quality` 调用数为 0。未篡改失败状态、重开原计费门禁、重新提交整课生成或重复生成视频。最后 40 条助手消息含 50 次工具操作，以逐项读取、核对和修改为主；真正清理 CDN 的部分为最后 8 个有工具轮次、11 次操作，三页清理均成功，不能描述成同一文件被反复重新注入的死循环。

本次还修正了两个程序问题：

- patch52：画面流为 5042ms，AAC 尾部使容器达到 5163ms。校验改为测量视频流，保留容器测量证据；真实视频 5250ms 仍拒绝。收据结构、SHA 和分辨率门禁不变。
- patch53：正式自包含互动页不再被后处理无条件插入 KaTeX CDN；普通生成的默认行为和已有数学处理保持。该程序冲突曾使模型额外清理三个没有公式的互动页。

这些修复已构建部署，但尚未再次通过一整次无人干预的正式生成。因此当前不能宣布自动流程问题全部解决。

## 可查看的预览

预览《集市里的圆点小侦探：20以内的数》包含 8 页、3 个互动页、4 道练习、1 张图片、1 段视频及 38 段讲解。内容边界为一年级 20 以内数感。

在保留原稿的副本上修正了散点布局、重置、配对和数线的状态响应，以及 16 点图形中的退化路径和聚光范围。圈十页面区分“已经数到”和“全部圆点”，避免尚未点亮的圆点被误解为不存在。教师、题目和语音文本的变更均有独立差异记录。

38 段语音及 38 次 ASR 使用本次原先尚未消耗的语音额度，独立限制器禁止进一步调用 LLM、搜索、图片和视频模型，继承原截止时间。该步骤只完成预览语音，不补造正式生成成功收据。音频总时长 297.6 秒，ASR 最低相似度 93.75%，所有音频实际字节、SHA、WAV/PCM 格式及动作对应均通过。

两种视口的互动检查通过；项目服务中的视频实际播放至结尾，分辨率 1280×720，无解码错误。最终预览文件 SHA256：`39447520e04e579c55ece29e2ef486c3f13cd3fdff7ff6d4c7c597485073ec82`。它是带人工修正的预览，不能用于证明正式评分、发布、完成状态或自动教学质量门禁已通过。

## 计费及验证证据

本次合计 158 次付费 POST：Pro 80、图片 1、视频 1、TTS 38、ASR 38。两个调用限制器都已封闭。视频提交未返回 usage，但其异步任务返回了实际 5 秒、720P 计量，不能按零费用计算。不同服务的字符、token 和秒数保留其原始口径，费用以百炼/DeepSeek账单为准。

关键回归包括后端 153 项、视频时长及相关运行时 33 项、正式互动依赖 24 项、原生启动测试，以及真实浏览器媒体和交互检查。安装后的 2914 份源码、124 份生成包文件与构建候选一致；真实编译代码已验证正式生成关闭外部 KaTeX 注入。

完整非敏感证据在 `output/openmaic-video-course-acceptance-2026-09-07/`，包括：

- `acceptance-manifest.json`：原自动失败与预览结果的区分。
- `guard-sealed-status.json`、`combined-paid-usage.json`：原任务封存及合并实际调用计量。
- `preview-audio-independent-verification.json`：38 段真实音频核验。
- `final-preview-classroom.json`、`final-preview-manifest.json`：可播放预览与最终文件身份。
- `final-preview-browser-verification.json`：项目服务中的实际操作观察。
- `production-post53-*-readonly.json`：新构建、队列及旧课程只读验收。

没有将原始计费账本中的临时签名媒体 URL 或任何 API Key 写入上述交付文件。
